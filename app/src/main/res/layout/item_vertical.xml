<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/light_primary"
    android:layout_marginBottom="20dp"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:id="@+id/llContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/ivNewsVertical"
        app:layout_constraintBottom_toBottomOf="@id/ivNewsVertical"
        app:layout_constraintEnd_toStartOf="@id/ivNewsVertical"
        android:layout_marginEnd="20dp"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Medium14Sp"
            tools:text="Minimal: Things you should know"
            android:lines="2"
            android:ellipsize="end" />

        <Space
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/tvAuthor"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Regular12Sp"
            tools:text="Iqbal Shafiq R"
            android:textColor="@color/light_disabled"
            android:lines="1"
            android:ellipsize="end" />

    </LinearLayout>

    <ImageView
        android:id="@+id/ivNewsVertical"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@drawable/bg_radius16dp"
        android:backgroundTint="@color/dark_accent" />


</androidx.constraintlayout.widget.ConstraintLayout>